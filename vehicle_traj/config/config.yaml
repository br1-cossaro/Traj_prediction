defaults:
  - data: waymo_debug
  - model: vehicle_transformer
  - train: base
  - task: base
  - inputs: base
  - _self_

seed: 0
device: cpu

eval:
  checkpoint: ???
  amp: false

hydra:
  run:
    dir: ${oc.env:TP_RUNS_DIR,trajectory_prediction/results}/vehicle_traj/${now:%Y-%m-%d}/${now:%H-%M-%S}
